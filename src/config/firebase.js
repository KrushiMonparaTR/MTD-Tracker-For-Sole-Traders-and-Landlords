// Demo mode configuration - set to true to run without Firebase
export const DEMO_MODE = true;

// Firebase configuration
// Replace these values with your actual Firebase project configuration
export const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "your-app-id"
};

// App ID for Firestore document paths
export const APP_ID = "mtd-tracker-app";

// Initial auth token (placeholder for future implementation)
export const INITIAL_AUTH_TOKEN = null;
